---
batch_connect:
  template: vnc
script:
  queue: "<%= bc_queue %>"
  native:
  - "--job-name"
  - "<%= bc_job_name %>"
  - "--nodes"
  - "<%= bc_num_nodes.to_i %>"
  - "--ntasks-per-node"
  - "<%= global_num_cores.to_i %>"
